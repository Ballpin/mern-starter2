import { asyncComponent } from 'react-async-component';

export default asyncComponent({
  // Include <%= name %> and about route in same chunk e.g main
  resolve: () => System.import(/* webpackChunkName: "main" */ './<%= helpers.capitalize(name) %>'),
});
